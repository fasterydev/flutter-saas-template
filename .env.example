# Clerk - copia este archivo a .env y rellena los valores
# O usa: flutter run --dart-define=CLERK_PUBLISHABLE_KEY=pk_test_xxxx

# Publishable Key (dashboard.clerk.com → API Keys)
# Empieza por pk_test_ (desarrollo) o pk_live_ (producción)
CLERK_PUBLISHABLE_KEY=pk_test_tu_clave_aqui

# URL del backend (API enterprise). Puedes usar rutas locales, ej:
#   http://localhost:3000
#   http://localhost:4000
# En emulador Android la app convierte localhost/127.0.0.1 → 10.0.2.2 automáticamente.
# En dispositivo Android físico usa la IP de tu PC (ej: http://192.168.1.10:3000).
BACKEND_URL=http://localhost:3000

# Opcional: si usas inicio de sesión con Google
# GOOGLE_CLIENT_ID=tu_google_client_id.apps.googleusercontent.com
